name: Temporary content index refinement

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: write

jobs:
  refine:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/content-index-localized-pages
          fetch-depth: 0

      - name: Fetch current main
        run: git fetch origin main

      - name: Install content-quality dependencies
        working-directory: tools/content-quality
        run: npm install --package-lock=false --ignore-scripts --no-audit --no-fund

      - name: Refine base semantic metadata
        run: node tools/content-quality/refine-index-temp.mjs

      - name: Validate content and index
        run: node tools/content-quality/lint-content.mjs

      - name: Commit refined index and remove temporary tooling
        run: |
          rm tools/content-quality/refine-index-temp.mjs
          rm .github/workflows/content-index-refine-temp.yml
          git add src/content/docs/CONTENT_INDEX.yml tools/content-quality/refine-index-temp.mjs .github/workflows/content-index-refine-temp.yml
          git diff --cached --check
          git config user.name "Antiokh"
          git config user.email "4112893+Antiokh@users.noreply.github.com"
          git commit -m "Refine base semantics in content index"
          git push origin HEAD:agent/content-index-localized-pages
